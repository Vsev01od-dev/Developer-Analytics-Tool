Metadata-Version: 2.4
Name: dev-analytics
Version: 1.0.0
Summary: Анализ эффективности работы разработчиков
Author: Vsevolod
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: tabulate>=0.9.0

## Developer Analytics Tool

Developer Analytics — это консольный инструмент для анализа эффективности работы разработчиков на основе данных из CSV-файлов. Скрипт формирует отчёты, рассчитывает метрики и предоставляет расширяемую архитектуру для добавления новых видов отчётов.

---

## Возможности

- **Чтение CSV файлов** с автоопределением разделителя (запятая, табуляция, точка с запятой)
- **Расчёт средней эффективности** разработчиков по должностям
- **Расширяемая система отчётов** через фабрику и абстрактные классы
- **Полное покрытие тестами** (92% по результатам pytest --cov)
- **Обработка ошибок** ввода и валидация данных
- **Красивый вывод** таблиц через библиотеку tabulate

---

## 🚀 Быстрый старт

### 1. Клонирование и установка
```
# Клонируйте репозиторий 
git clone https://github.com/Vsev01od-dev/Developer-Analytics-Tool.git
cd Developer-Analytics-Tool

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Базовый запуск
```
# Откройте терминал в корне проекта

# Способ 1: Прямой вызов модуля
python -m script.cli --files data/employees1.csv --report performance

# Способ 2: Через установленный пакет (если используете pyproject.toml)
pip install -e .
dev --files data/employees1.csv --report performance
```

---

## 📊 Доступные команды

### Основные команды
```
# Отчёт по эффективности (основной)
dev --files data/employees1.csv --report performance

# Несколько файлов одновременно
dev --files data/employees1.csv data/employees2.csv --report performance

# Пример нового отчёта (нужно раскомментировать)
dev --files data/employees1.csv --report skills
```
### Тестирование и качество кода
```
# Запуск всех тестов с подробным выводом
pytest -v

# Проверка покрытия кода
pytest --cov=script --cov-report=html

# Форматирование кода (black)
black script/ tests/

# Сортировка импортов (isort)
isort script/ tests/
```

---

## 📈 Примеры работы

### 1. Отчёт по эффективности (performance)
![1](images/1.png)

### 2. Отчёт по количеству сотрудников (skills)
![2](images/2.png)

---

## 🏗️ Архитектура проекта

Проект построен по модульному принципу с поддержкой расширения:

```
pythonProject11/
├── script/                    # Основной код
│   ├── cli.py               # Точка входа, обработка аргументов
│   ├── reader.py            # Чтение и парсинг CSV
│   ├── reports.py           # Система отчётов (фабрика + абстрактные классы)
│   └── processors.py        # Утилиты обработки данных
├── tests/                   # Полный набор тестов
├── data/                   # Примеры CSV файлов
└── pyproject.toml          # Конфигурация проекта и зависимостей
```

---

## 📁 Формат CSV файлов

Поддерживаются файлы со столбцами:

```
name,position,completed_tasks,performance,skills,team,experience_years
```

Разделители определяются автоматически: `,` `\t` `;`

---

## ✨ Добавление новых отчётов

Архитектура позволяет легко добавлять новые типы отчётов:

### Шаг 1: Создайте новый класс отчёта

```
# В reports.py
class NewReport(Report):
    def generate(self, data):
        # Ваша логика обработки данных
        return processed_data
    
    def display(self, report_data):
        # Ваша логика вывода
        print(report_data)
```

### Шаг 2: Зарегистрируйте отчёт

```
# В reports.py
ReportFactory.register_report('new_report_name', NewReport)
```

### Шаг 3: Используйте

```
dev --files data.csv --report new_report_name
```

**Пример:** В коде уже есть закомментированный SkillsReport для отчёта по количеству сотрудников.

---

## 🔧 Инструменты разработки

### Pyproject.toml

Центральный файл конфигурации содержит:

- **Метаданные проекта** (имя, версия, зависимости)
- **Настройки тестирования** (pytest с покрытием кода)
- **Конфигурацию линтеров** (black, isort)
- **Точки входа** (консольная команда dev)

### Доступные команды для разработки

```
# Установка в режиме разработки (создаёт команду 'dev')
pip install -e .

# Проверка покрытия тестами
pytest --cov=script --cov-report=term-missing

# Форматирование всего кода
black . && isort .

# Запуск всех проверок
pytest && black --check . && isort --check .
```

---

## 🧪 Тестирование

Проект имеет **покрытие тестами 92%** (по данным pytest-cov).

### Структура тестов

```
tests/
├── test_cli.py          # Тесты CLI интерфейса
├── test_reader.py       # Тесты чтения CSV
├── test_reports.py      # Тесты системы отчётов
└── test_processors.py   # Тесты утилит обработки
```

### Запуск тестов

```
# Все тесты с детальным выводом
pytest -v

# Конкретный тестовый файл
pytest tests/test_reader.py -v

# Создание HTML отчёта о покрытии
pytest --cov=script --cov-report=html
# Отчёт будет в htmlcov/index.html
```

---

## ⚙️ Обработка ошибок

Скрипт обрабатывает следующие ошибки:
- ❌ Файл не найден — с указанием отсутствующих файлов
- ❌ Неверный формат CSV — неправильные разделители, типы данных
- ❌ Неизвестный отчёт — отображение доступных отчётов 
- ❌ Проблемы с данными — нечисловые значения в числовых полях

---

## 📄 Лицензия

Этот проект создан в рамках тестового задания. Код может быть использован в учебных целях.
